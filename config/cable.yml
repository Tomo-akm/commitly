# Use async adapter in development (solid_cable is for production only)
development:
  adapter: async

test:
  adapter: test

production:
  adapter: solid_cable
  # Solid Cable uses the primary database (no separate cable database needed)
  polling_interval: 0.1.seconds
  message_retention: 1.day
