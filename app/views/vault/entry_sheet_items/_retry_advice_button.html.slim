= button_to vault_entry_sheet_item_advice_path(item), method: :delete, class: "btn btn-sm btn-outline-secondary", form: { data: { turbo_confirm: "既存の添削結果を削除して再添削しますか？" } }, id: "retry_advice_button_#{item.id}" do
  i.fas.fa-redo.me-1
  | 再添削